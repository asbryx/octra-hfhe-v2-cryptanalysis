# Legacy probes and raw outputs

Historical source probes, measured JSON/text output, and build diagnostics retained for auditability.

These files are not the canonical current workflow. Some target the announcement snapshot `0d08e96`, use reduced or deterministic fixtures, or contain verdict wording later narrowed. Use the top-level `tools/`, `results/`, `STATUS.md`, and `research/final-exhaustion.md` for current conclusions.

Generated executables, Python bytecode, and downloaded upstream artifacts are excluded. Rebuild from source when needed.
